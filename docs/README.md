# Docs

Guides that go beyond the top-level [README](../README.md).

- [ARCHITECTURE.md](ARCHITECTURE.md): how the backend workspace is split into `core` and
  `crates/atk-*`, and why the boundaries fall where they do.
- [RELEASING.md](RELEASING.md): the maintainer process for cutting a release and updating the
  Homebrew formula.
