name: Build Check
on:
  push:
    branches: ["**"]
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  build:
    runs_on: self-hosted
    steps:
      - name: Build workspace
        run: cargo build --workspace --all-targets
